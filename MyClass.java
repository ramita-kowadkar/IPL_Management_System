package oep;
import java.util.*;


public class MyClass {
	public static void main(String[] args) {
		
		/*Scanner input = new Scanner(System.in);
		String name;
		int choice = 0;
		
		Points_Table points = new Points_Table();
		History history = new History();
		Person person = new Person();
		
		while(choice < 5) {
			System.out.println("\nMenu of operations :");
			System.out.println("\n 1. Person \n 2. Points Table \n 3. History \n 4. Exit");
			System.out.print("Enter the operation to be performed : ");
			choice = input.nextInt();
			switch(choice) {
				case 1 : System.out.print("Enter the name of the person : ");
						 name = input.next();
						 person.personName(name);
						 break;
				case 2 : for(int i = 0; i < 8; i++)
							{
								System.out.print("Enter name of the team : ");
								teamname[i] = input.next();
								System.out.print("Enter number of matches won : ");
								int won = input.nextInt();
								System.out.print("Enter number of matches lost : ");
								int lost = input.nextInt();
								System.out.print("Enter number of tie matches : ");
								int tie = input.nextInt();
								System.out.println();
								score[i] = points.computeScore(won,lost,tie);
							}
						 points.sortScore(score,teamname);
						 break;
				case 3 : history.dispHistory();
						 break;
				case 4 :System.exit(0);
			}
		}*/
		
	}
}